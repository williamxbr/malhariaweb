@model IEnumerable<MalhariaWeb.Models.ORDEM_ACABAMENTO_PECAS>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/_LayoutMalhariaWeb.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_ORDEM_ACABAMENTO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID_PECA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PESO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.METROS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_ORDEM_ACABAMENTO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ID_PECA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PESO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.METROS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_ORDEM_ACABAMENTO_PECAS }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_ORDEM_ACABAMENTO_PECAS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_ORDEM_ACABAMENTO_PECAS })
        </td>
    </tr>
}

</table>
