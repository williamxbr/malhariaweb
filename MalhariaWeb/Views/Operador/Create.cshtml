@model MalhariaWeb.Models.OPERADOR

@{
    Layout = "~/Views/_LayoutMalhariaWeb.cshtml";
    ViewBag.Title = "Create";
}

<div class="row">
    <div class="col-md-12">

        <ul class="breadcrumb">
            <li>@Html.ActionLink("Home", "Index", "Home")</li>
            <li class="active">Cadastro</li>
            <li>Operador</li>
        </ul>

        @using (Html.BeginForm("Create", "Operador", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horinzontal", role = "role" }))
        {
            @Html.ValidationSummary(true)

            <fieldset>

                <div class="form-group">
                    @Html.LabelFor(model => model.NOME_OPERADOR, new { @class = "col-md-2 control-label" })
                    @Html.TextBoxFor(model => model.NOME_OPERADOR, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.NOME_OPERADOR)
                </div>

                <p>
                    <button class="btn btn-success" type="submit" value="Salvar">Create</button>
                </p>
            </fieldset>
        }

    </div>
</div>

